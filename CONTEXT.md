# typeapp 开发与分发

本语境描述 TypeApp 应用框架、TypePHP 编译器、Swoole 原生运行能力和 Plugins 组件的关系，以及业务应用的开发与分发。物联中心是成品案例，不是框架本身。

## 术语

**typeapp**：面向原生交付的 PHP 应用框架，统一业务开发、组件装配、全量编译与应用运行约定。仅描述文案可写作 TypeApp。
_避免混用_：TypePHP 编译器、Swoole 扩展本身、物联中心

**TypePHP**：TypeApp 采用的 PHP 预先编译（AOT）工具，将应用的生产 PHP 实现编译为原生代码。
_避免混用_：TypeApp 框架、Swoole 原生扩展、运行时 PHP 解释器

**Swoole**：随 TypeApp 应用交付的必需原生运行库，通过扩展接口提供网络通信、进程、线程、协程与事件循环能力；由框架接入，由构建管理。
_避免混用_：TypePHP 编译器、Plugins 组件包、TypeApp 全部运行依赖

**Plugins**：TypeApp 中由 Composer 管理的 `type-xxxx` 框架组件的统称，包含运行组件和开发工具组件。
_避免混用_：Swoole 原生扩展、TypePHP 编译器插件、Composer 安装器插件、运行时热插拔模块

**开发主仓**：集中维护 TypeApp 框架、Plugins 与测试的仓库。本项目主仓名为 typeapp，并附带成品案例物联中心。
_避免混用_：应用模板仓库、插件子仓、物联中心等于框架

**业务应用**：依赖框架组件公开能力、承载实际业务规则的应用。可用 `type-project` 创建，按需安装 `type-xxxx` 组件。物联中心是成品案例，不是唯一业务。
_避免混用_：框架组件、组件分发子仓、仅供测试的夹具

**成品案例**：主仓随仓库提供的完整业务应用，当前是物联中心。用来展示 TypeApp 如何装配成可运行产品，不是框架本身。
_避免混用_：TypeApp 框架、只有目录的空骨架、已通过全部验收的声明

**标准项目**：同「成品案例」。现为 `app/` 中的物联中心。
_避免混用_：TypeApp 框架、只有目录的空骨架、已通过全部验收的声明

**组件包**：Plugins 中的单个 Composer 包，有独立依赖声明和使用入口；“插件包”指同一概念。
_避免混用_：Codex 插件、Git 子模块、默认等同于 Composer 安装器插件

**分发子仓**：承接一个插件包的独立发布仓库，供插件使用者获取对应版本。
_避免混用_：开发主仓的子目录、Git 子模块

**运行发布包**：面向部署的应用交付物；约定形态为一个主程序文件加外置配置，非系统原生运行库静态链接进程序，启动不释放运行库。
_避免混用_：开发主仓、组件包、源项目、目录包压缩成的归档、已通过单文件验收的声明

**应用模板**：用于创建独立业务应用的初始项目；本项目由 `type-project` 承担。模板提供 TypePHP 构建、入口与最小示例，不默认包含物联中心业务。
_避免混用_：开发主仓、成品案例物联中心、框架全部源码

**分发批次**：从一个确定的主仓源码状态生成并发布一组插件版本的整体记录。
_避免混用_：单个子仓提交、业务部署

**完整交付**：同一约定范围内所有框架功能、数据库支持、测试和发布能力共同完成的交付。
_避免混用_：目录骨架完成、某个插件局部验证成功

**正式支持目标**：TypeApp 完整开发、构建与运行承诺所覆盖的环境组合；是否已通过验收，以对应版本的实际证据为准。
_避免混用_：TypePHP 存在平台实现、上游发行包可下载、框架已经通过验收

**运行时基础能力**：TypeApp 基于 Swoole 向业务角色提供的执行与资源管理能力，包含线程、协程、执行隔离、资源所有权和有界生命周期的统一契约。
_避免混用_：某个业务组件的并发选项、原生 I/O 辅助线程池、全部平台已验收

**Swoole 成熟基准**：TypeApp 必须采用的官方通信与并发机制，用来减少重复实现及开发、适配和维护成本。
_避免混用_：可选通信驱动、已优于所有框架的声明、自研事件循环

**基础通信能力**：TypeApp 面向通用应用提供的 HTTP、TCP、UDP、MQTT 与 WebSocket 客户端及服务端能力，共用连接与执行资源的生命周期契约，同时保留各协议的消息和失败语义。
_避免混用_：只有 HTTP 服务、MQTT 专用传输、统一成同一种报文格式

**Transport**：面向应用的协议通信能力，当前包括 HTTP、TCP、UDP、MQTT 与 WebSocket；它描述数据交换语义，不等同于监听器或执行单元。
_避免混用_：Server、网络连接、统一报文协议

**Server**：拥有监听地址、端口和协议回调的服务端对象；HTTP 与 WebSocket 共用端口时由同一个 Swoole Server 持有监听所有权。
_避免混用_：Transport、Role、多个对象同时拥有同一监听

**Client**：主动建立外部连接并执行协议收发的客户端对象，连接和资源归属于创建它的执行范围。
_避免混用_：设备主体、Server、共享全局连接

**Role**：承载 Server、Worker 或 Scheduler 等工作的进程、线程或协程执行单元，拥有明确的资源和停止责任。
_避免混用_：Transport、Server、业务身份、平台支持声明

**执行上下文**：一次请求、消息、命令或后台任务在一个执行单元中的关联信息、截止时间、取消意图和资源预算。
_避免混用_：进程全局变量、登录身份本身、单个网络连接

**协程上下文**：绑定单个 Swoole 协程的执行状态，用于在协程切换后继续识别当前工作、传递受控关联信息并校验资源归属；它不等于进程级共享状态。
_避免混用_：协程 ID、HTTP 会话、可跨协程共享的可变对象

**执行作用域**：执行上下文的生命周期边界；作用域负责拒绝新工作、取消子任务、等待真实收尾并按逆序释放资源。
_避免混用_：容器生命周期、网络连接本身、可以无限延长的后台任务

**资源所有权**：资源只能由登记它的进程、线程请求和协程执行者使用，跨边界只能传递受控值或重新建立资源。
_避免混用_：把连接、事务或业务对象放进全局单例、跨线程复制 Zend 对象

**Application**：组合配置、业务服务、Transport、Server、Client 与 Role 的业务应用整体。
_避免混用_：TypeApp 框架、单个 Plugin、单一监听服务

**显式装配**：由应用入口直接构造并连接所需组件，明确声明依赖、资源所有者和关闭顺序的装配方式；生产运行不依赖通用运行时容器、未知源码扫描或隐式服务发现。
_避免混用_：运行时 DI、自动扫描、组件热插拔

**Model（数据模型）**：业务持久实体的标准读写对象，承载字段状态、赋值约束、关系、删除和版本语义，并作为业务 CRUD 的首要入口。
_避免混用_：物模型、数据库表对象、直接暴露的 PDO 行数组

**ModelQuery（模型查询）**：围绕具体 Model 组合不可变过滤、投影、排序、分页、关系和聚合的查询表达，结果保持模型语义或明确的聚合投影。
_避免混用_：任意表查询、可变查询构造器、数据库连接本身

**模型操作入口**：业务通过具体 Model 的查询、创建、查找和实例保存/删除能力完成常用 CRUD；连接、租户范围和数据源路由由执行上下文与 ORM 管理。
_避免混用_：传递 PDO 或 Connection 的业务方法、按表名拼装的通用 CRUD、把请求参数直接当作 SQL 条件

**模型生命周期**：Model 从创建或查询水合开始，经赋值、保存、删除和失效校验完成一次有边界的持久化过程；已加载模型保留其身份与归属，不能跨租户或执行作用域继续使用。
_避免混用_：数据库行数组、可跨请求共享的活动连接、脱离归属的缓存实体

**批量模型操作**：在 ModelQuery 上对一组记录执行明确的更新、删除或算术操作并返回影响数量；它不逐条水合 Model，也不承诺逐条实例钩子和领域副作用。
_避免混用_：逐条 Model 保存、隐式 N+1、声称批量 SQL 自动具备逐模型事件语义

**显式关系加载**：通过查询预加载或对已有模型批量补加载关系，关系属性只表示已加载结果；未声明加载不会隐式访问数据库。
_避免混用_：访问属性即自动查询、跨作用域共享关系对象、脱离父查询路由的单独连接

**白名单搜索**：调用者提供输入后，按代码中声明的字段映射显式组合等值、模糊、排序或分页条件；搜索入口不读取全局请求，也不推断未知字段。
_避免混用_：任意请求参数查询、自动授权过滤、把搜索助手当作模型查询或业务权限组件

**QueryHelper（模型与表查询筛选助手）**：在已有查询上按开发者声明的字段映射组合显式输入的筛选与排序，也提供有界分页入口；它不决定业务授权或租户归属。
_避免混用_：ModelQuery、自动读取请求的查询器、只组合条件而不会执行的对象

**Db（数据库事务入口）**：业务对当前模型数据源表达事务与提交后操作的统一入口，连接所有权仍属于执行作用域。
_避免混用_：PDO 连接、另一套连接池、跨数据库原子事务

**模型 CRUD**：以具体 Model 或 ModelQuery 完成的创建、读取、修改和删除操作，遵守字段赋值、输出、软删除、版本和业务归属边界；连接的取得与释放归属于当前执行作用域。
_避免混用_：直接按表名执行的通用 CRUD、绕过模型状态的批量 SQL

**模型数据源**：领域 Model 所属的逻辑数据库，具有独立的连接配置、主从路由与事务边界。
_避免混用_：物理 PDO 连接、租户身份、一次事务、应用部署副本

**表查询**：面向数据库表或跨表结果的底层查询能力，用于迁移、基础设施、复杂投影/聚合和数据库能力探测等明确例外，不作为业务实体 CRUD 的默认入口。
_避免混用_：模型查询、业务服务的默认数据访问、任意 SQL 逃生口

**模型优先**：业务代码先选择具体 Model 及其 ModelQuery；只有无法表达的基础设施或跨模型场景才使用表查询，并保留可审查的职责边界。
_避免混用_：所有场景强行套模型、以通用 Repository 隐藏连接和查询语义

**领域 Model**：归属于一个业务领域、具有稳定表映射和业务字段语义的具体 Model，是该领域服务执行实体 CRUD 的公开数据入口。
_避免混用_：按运行时字符串切换表名的动态模型、只为迁移临时创建的模型

**租户上下文**：当前执行工作所属的租户身份，由应用在验证访问资格后建立，供模型自动识别数据归属。
_避免混用_：未经验证的请求参数、数据库连接身份、登录账号本身

**租户范围查询**：具有指定租户字段的模型依据租户上下文自动限定数据归属的查询，读取与持久化共同遵守这一边界。
_避免混用_：调用者逐次补租户条件、仅按裸主键访问租户数据、拥有租户身份即拥有全部业务权限

**基础设施表查询**：由迁移、ORM 基础设施、审计/Outbox/运行观测或数据库能力探测承担的受限表级访问，不能替代业务实体的 Model CRUD。
_避免混用_：业务 Service 的默认数据访问、任意 SQL 逃生口、模型关系查询

**应用生命周期**：应用从 `configure`、`start` 到 `stop` 的最小运行边界；配置和对象构造不打开外部资源，启动失败必须回收已启动资源，停止必须先拒绝新工作再按期限排空。
_避免混用_：单个请求生命周期、协议状态机、无限等待

**编译声明**：由构建工具读取并生成显式调用代码的路由、缓存或事务信息；声明本身不在运行时拦截原始方法。
_避免混用_：运行时 AOP、解释执行配置、自动扫描未知源码

**物联网中心**：TypeApp 的成品案例，由主仓 `app/` 实现，统一管理设备身份、接入消息、遥测数据和下行指令。
_避免混用_：TypeApp 框架、单个协议接入组件、MQTT Broker、唯一业务形态

**Web 管理端**：物联网中心的浏览器应用，包含平台管理端与 SaaS 用户端。
_避免混用_：仅平台管理端、设备接入端、设备固件

**平台管理端**：面向平台管理人员的业务模块，管理平台人员、客户账号、租户、设备资产及全局配置。
_避免混用_：租户内的最高管理员、独立 Broker 管理端、SaaS 用户端

**SaaS 用户端**：面向客户账号的默认业务入口，按当前租户的成员关系与角色管理用户及物联业务数据。
_避免混用_：平台管理端、仅提供查询的门户、一个账号等于一个租户

**管理账号**：仅用于平台管理端的人员身份，通过平台角色取得平台范围的权限。
_避免混用_：客户账号、设备凭据、租户最高管理员

**客户账号**：用于 SaaS 用户端的人员身份，可以通过不同成员关系加入多个租户。
_避免混用_：租户、管理账号、只属于一个租户的成员关系

**租户成员**：客户账号与某一租户之间的关系，承载该租户内的成员状态及角色分配。
_避免混用_：全局客户账号、其他租户的成员、平台角色

**平台角色**：管理账号在平台范围内的一组权限，同一管理账号的多个有效平台角色共同决定其平台权限。
_避免混用_：租户角色、固定职位、客户账号的业务权限

**租户角色**：由某一租户独立管理的一组权限，该租户成员的多个有效角色共同决定其租户内权限。
_避免混用_：跨租户共享角色、平台角色、登录身份

**权限目录**：应用固定定义、分别面向平台管理端和 SaaS 用户端的页面与业务动作清单，是角色授权的可选范围。
_避免混用_：角色已有权限、可任意创建的运行时节点、仅控制菜单显示

**模拟登录**：有权管理人员直接进入客户账号工作区的操作身份，业务权限随目标客户当前租户角色确定，同时保留真实管理人员来源。
_避免混用_：客户提供密码、客户限时授予的支持身份、合并平台和租户权限

**租户**：物联网中心内拥有独立设备归属与业务数据的组织主体。
_避免混用_：登录用户、管理角色、设备分组

**设备**：具有独立身份、能够上报数据或执行指令的接入对象，同一时刻只归属于一个租户。
_避免混用_：网络连接、产品型号、登录用户

**产品**：租户拥有的一类设备的管理定义，为关联设备提供物模型及其版本。
_避免混用_：单台设备、租户、硬件库存商品

**物模型**：描述某类设备属性、事件、指令及其数据约束的业务契约。
_避免混用_：数据库模型、设备当前数据、MQTT 协议

**物模型版本**：一份具有明确版本标识的物模型定义，是设备关联和业务数据解释的依据。
_避免混用_：固件版本、MQTT 协议版本、产品名称

**MQTT 服务**：面向标准 MQTT 客户端提供连接、会话和消息路由的独立服务端，亦称 Broker；物联网中心是其使用方之一。
_避免混用_：MQTT 客户端库、仅解析设备 JSON 的接收接口、物联网中心全部业务

**Broker 管理端**：供管理人员查看和管理 MQTT 服务运行状态的 Web 管理模块。
_避免混用_：设备业务管理页面、MQTT 协议接入端、MQTT 客户端

**Broker 接入主体**：经认证使用 MQTT 服务的设备、人员或服务的稳定身份，是其接入授权的归属对象。
_避免混用_：Client ID、网络连接、单张证书、某次签发的凭据

**Broker 接入凭据**：用于证明 Broker 接入主体身份的账号凭据、已登记证书或短期 MQTT 凭据；更换凭据不等于更换主体。
_避免混用_：Client ID、人员管理端登录令牌、主体本身

**设备转移**：设备归属从一个租户变为另一个租户的显式业务操作。
_避免混用_：设备重连、设备分组调整、复制设备档案

**归属阶段**：设备在一次确定的租户归属下产生业务数据和接受控制的身份阶段；设备转移后形成新的阶段。
_避免混用_：租户 ID、MQTT 会话、物模型版本

**设备生命周期**：设备在管理上所处的未激活、启用、禁用或退役状态。
_避免混用_：连接在线或离线、数据新鲜度、转移进度

**数据陈旧**：设备未在约定时间内提供有效新遥测的状态。
_避免混用_：设备确定离线、Broker 节点暂不可达、页面刷新失败

**设备执行回执**：设备返回的指令执行结果信息。
_避免混用_：MQTT 协议确认、接入端接受凭据、平台接口调用成功

**平台持久接收回执**：平台向设备确认某条业务上报已经达到约定持久接收边界的信息，是设备清理该条本地缓存的依据。
_避免混用_：MQTT 协议确认、设备执行回执、后续业务全部处理完成

**指令结果未知**：平台缺少足够证据，无法确定设备是否执行指令及其执行结果的状态。
_避免混用_：设备未执行、执行失败、指令已取消

**告警确认**：人员已知悉并确认处理某条告警的业务记录。
_避免混用_：设备数值恢复、告警自动消失

**告警恢复**：设备数据重新满足规则规定的恢复条件，使活动告警结束的业务变化。
_避免混用_：人员确认告警、设备没有继续上报
