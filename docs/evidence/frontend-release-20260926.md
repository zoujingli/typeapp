# 前端内嵌与版本发布本机验收

本记录为 2026-09-26 的本机实施验收，不替代首次 RC 的四平台 Actions、Packagist 和公开 Release 验收。测试基于 `2d4503f77b8eced67ac57e502b4d4c9bb7b15fa9` 加本次工作区修改；原始补丁、新文件及编译输入清单随证据保全，不将产物身份回写为之后的提交。

## 源码与产物身份

| 项目 | 本轮值 |
| --- | --- |
| 平台 | macOS ARM64 |
| 工具链 | TypePHP 0.9.3、PHPX 2.9.2、PHP 8.5.10 ZTS；准确提交取保全的锁文件 |
| 全量应用编译单元 | 275 |
| 隔离构建输入摘要 | `e39772fd49fccfd6ba1f43f96645ddb8e82a768cd387618be4c63c0de034a7a4` |
| 应用构建 ID | `0cf6167822f4894a0e8e0521658810a944349545699125da2d7eb75b2a1f1a49` |
| 主程序 SHA-256 | `038c696e7a49913a9e8486a8a047acbd0079cdef68e7d768626f320bfc4feb83` |
| 最终目录归档 SHA-256 | `dea4731e706aefda891d07c1a01eefadb15d92f474ff7d8c7847b70c7075266b` |
| 归档字节数 | 22402976 |
| release.json SHA-256 | `dbe8570c94031dceb0705cbd6b77faf80b77ed990520197eda209da6fab29b05` |

编译快照中的 `app/` 与 `plugin/type-build/src/` 在验收完成时和工作区逐文件一致。本机产物版本仍为开发版本，不作为 `v1.0.0-rc.1` 的下载附件。

## 已执行结果

- 冻结前端依赖安装、类型检查与生产构建通过。资源由 TypePHP 原生桥接编入应用，原始许可材料保留。
- 格式、基础、文档、工作流语法及分发检查通过；完整单元套件为 130 个测试、3145 个断言。Git 分发批次回归覆盖部分失败、同一身份重试与不可移动标签；版本门禁覆盖无效版本、错误 SHA、缺失任务、模板 tag 和依赖冲突。
- 独立内嵌资源 AOT 在移走源资源后通过二进制字节、空文件、分块边界、安装、重复执行、强制更新、含空格路径、不同工作目录和页面读取。安装单元回归另外覆盖旧资源清理、非托管文件保留、只读目录、篡改、互斥和恢复记录。
- 同一最终归档重新解包后，在 SQLite、MySQL 8.4.11、PostgreSQL 17.11 逐一通过部署回归；macOS sandbox 拒绝源码、SDK 与 Composer 读取和 PHP/编译器执行。检查全部内嵌文件摘要、安装与更新、上传保留、GET/HEAD、ETag 304、错误方法 405、未知 API 404、双端登录、站点默认信息、角色 CRUD、正常停止及篡改拒绝。
- 同一产物的三库原生备份恢复通过。恢复到新应用根后单独执行 `web:install` 安装页面，原生数据库服务在测试结束时停止。
- Docsify 导出与引用检查通过；浏览器确认发布流程图、安装时序图、命令表及备案标题正常显示。仅使用任务临时预览，没有替换本机业务站点。

## 限制与后续验收

本机结果只覆盖 macOS ARM64。Linux x64、Linux ARM64、Windows x64 的本轮结果及 17 个 Release、16 个子仓标签、16 个 Packagist 版本须由远端真实运行另行记录。HTTP 回归通过真实页面资源和业务 API，不等同于浏览器逐项操作所有管理页面。

当前附件仍是包含原生运行库的目录归档；页面在显式安装时写到 `public/`，普通启动不释放资源。完整静态单程序目标未完成。Windows 现有部署隔离也不等同于 macOS/Linux 的强制拒绝工具访问边界。

## 证据保全

本地受控归档为 `.cache/retained-evidence/frontend-release-20260926/local-acceptance.tar.gz`，SHA-256 为 `5d833df4881d5ea3abfebad16724191e18a1423f583cd098d9b215bc22dc4378`。其中 69 个原始文件逐项回读核对摘要，包含锁文件、源码补丁与新增实现、完整输入身份、最终运行归档和原始成功/失败日志；早期失败与中间产物不冒充最后通过的结果。

需要复核时，先校验归档摘要，再解压到新建的专用目录，按归档内 `evidence-files.json` 核对原相对路径及逐文件摘要。该归档不公开分发，保留至首次 RC 证据完成审核；共享 SDK 和日常业务数据不属于本任务清理范围。
