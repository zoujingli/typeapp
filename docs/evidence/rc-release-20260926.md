# 首次 RC 自动发布验收

本记录区分发布前的四平台预检与版本 tag 的正式发布，两个阶段保留各自的源码身份。首次 `v1.0.0-rc.1` 的不可变 tag 指向 `9c8febf11b9997f3a76779825b3c7b2fb2fbbfb2`；[版本工作流](https://github.com/zoujingli/typeapp/actions/runs/36224276504)在 Windows 最终候选部署验收失败，没有进入子仓分发或公开 Release。修复使用后续提交和新的候选版本，保留原标签及失败证据。

## 发布前检查

源码 `48fc8df7997e85f029e351860da9537ee64f3d25` 的四平台完整矩阵通过。这一阶段没有生成公开 RC 附件，结果不能替代最终 tag 的验收。

| 平台 | Actions 运行 | 已完成范围 |
| --- | --- | --- |
| Linux x64 | [36216392545](https://github.com/zoujingli/typeapp/actions/runs/36216392545) | 19 个分组及完整矩阵门禁 |
| Linux ARM64 | [36216424559](https://github.com/zoujingli/typeapp/actions/runs/36216424559) | 9 个分组及完整矩阵门禁 |
| macOS ARM64 | [36216392538](https://github.com/zoujingli/typeapp/actions/runs/36216392538) | 8 个分组及完整矩阵门禁 |
| Windows x64 | [36216392536](https://github.com/zoujingli/typeapp/actions/runs/36216392536) | 完整契约、三库 ORM、应用、搬迁与模板验收 |

随后提交两项发布工具修正：候选草稿预检取得读取草稿所需的仓库写权限；组件 RC 安装说明显式设置根项目 `minimum-stability RC` 和 `prefer-stable`，允许解析候选传递依赖。PHP 语法、格式、工作流语法和发布契约检查通过，定向契约套件为 86 个测试、1906 个断言。独立 Composer 离线夹具确认默认稳定性拒绝 RC 传递依赖，设置 RC 后两个候选包均按指定版本安装。

主仓与 16 个子仓在打 tag 前没有同名版本；子仓 Packagist push webhook 均启用，最近可观察响应均为 HTTP 202。正式发布仍须核对实际索引版本，不能用 webhook 配置代替消费结果。

## 执行与交付边界

推送最终 `main` 后创建并推送 `v1.0.0-rc.1`。同一提交额外触发的三项主分支构建已取消，以版本工作流执行完整四平台矩阵；取消的任务不计为通过。版本检查与共享前端构建已通过，后续阶段仍以真实运行结果为准。

Windows 候选 `prepare` 已完成打包、解包与完整性检查，随后 SQLite 验收约 0.2 秒退出。测试入口将 Windows `realpath()` 的反斜杠路径与包含正斜杠的构建目录前缀直接比较，错误拒绝了合法候选目录；定向回归在原比较逻辑下失败。修复统一 Windows 盘符与 UNC 路径分隔符，同时保留目录边界及 Unix 文件名语义，9 个路径用例通过。原日志上传范围遗漏候选内层错误，现已补入四平台失败证据；真实 Windows 补验仍按新的运行身份记录。

修复提交为 `2caa97cb78795bbc5ad333646c68e3b494d69c87`，本机完整单元套件通过 140 个测试、3160 个断言，格式、基础、文档和工作流检查通过。短暂启动的 Windows 手动候选诊断没有完成验收，随后取消；`rc.1` 中未完成的 macOS 分组也在保留运行状态后取消，不计为通过。新不可变标签 `v1.0.0-rc.2` 指向修复提交，由[新版本工作流](https://github.com/zoujingli/typeapp/actions/runs/36228227822)重新执行完整四平台及最终归档验收，不复用 `rc.1` 的候选文件或拼接原运行结果。

`rc.2` 的 Linux x64 与 ARM64 完整矩阵通过，macOS 已完成两个分组。对其 macOS 候选补充浏览器验收时，发现静态资源 `HEAD` 返回 `Content-Length: 0`，对应 `GET` 的脚本实际为 91 字节。页面响应提前换成空流，发送层按流大小生成长度，造成两者不一致；原候选验收只检查空正文，没有检查长度。为阻止不合格候选公开，取消其余运行。17 个仓库均无该版本 Release，16 个子仓均无该版本 tag；取消项和因取消而失败的聚合门禁不计为通过。

修复提交 `09cb1ec746ba88eb3e139cf332cefa1b644567f5` 保留文件流，由既有发送层统一省略 `HEAD` 正文并关闭流；新增首页和脚本的真实 HTTP 长度、类型及缓存标识校验。原逻辑的定向回归失败，修复后通过；完整单元为 140 个测试、3161 个断言。本机全量 AOT 编译 275 个单元，程序 SHA-256 为 `89408e2e0a4b8c0e6d0f7a889529af114bbf6edb6882d33f5cb3c86232774c7d`，其搬迁包通过隔离 SQLite 发布验收，包含真实 `HEAD` 长度及完整部署审计；本机产物不是公开 RC 附件。新标签 `v1.0.0-rc.3` 指向该修复提交，后续四平台产物重新编译并独立验收，不能替换 `rc.2` 原件。

`rc.2` 的 macOS 候选浏览器补验另有明确边界：原归档来自 macOS 15，本机为 macOS 27，`verify-runtime` 的系统 dyld 缓存审计因摘要不同失败。普通入口的运行库校验通过，隔离 SQLite 中的安装、双端登录、站点设置保存回读、角色增改查及 API 删除回读通过，83 个安装文件摘要一致，服务正常停止；这些功能结果不能替代完整部署审计，也不构成 macOS 27 的完整支持声明。最初补验脚本使用了沙箱不可读的工作目录，修正为允许的独立运行目录后才取得上述真实结果，原错误日志单独保留。

## RC3 原候选补验

`v1.0.0-rc.3` 的[正式工作流](https://github.com/zoujingli/typeapp/actions/runs/36230932433)固定源码 `09cb1ec746ba88eb3e139cf332cefa1b644567f5`。共享前端为 83 个文件，清单 SHA-256 为 `8cf440def909935206893729e6fcbee2317ef617aeee7959344c165f338d09f8`。

本轮 Linux x64 与 ARM64 完整矩阵通过；Windows 的最终 ZIP 在 PostgreSQL 空库 `app:install` 中断，外层日志仅见 `^C`，尚未完成全部验收。相同运行的 Windows 开发应用、AOT 应用及模板 PostgreSQL 检查已通过，SQLite 与 MySQL 候选检查也通过。保留失败原件后取消剩余 macOS 分组，未进入候选草稿、子仓 tag 分发或 Release 公开；取消项不计通过。安装测试使用 20 秒内层预算，但原报告未记录内层超时状态，不能仅凭 `^C` 断定根因，后续用原 ZIP 定向测量。

Windows 原候选 ZIP 的 SHA-256 为 `41f373594347b734c6a98c8dd121511ae3b9540149a8dce458b66fb7b05176bf`，程序 SHA-256 为 `a2b9fb07f3d664c30a500c8e0b437478110906fac79047ecf25fda74d412ebe6`。原 `windows-native-evidence` 附件摘要为 `3cf4ef3fbe82c1637cbb074640f49d39234ae7a1208d682f84b7e786658335f7`，下载后按 GitHub 记录回读核对；诊断不重编译或替换这些原件。

[原候选定向测量](https://github.com/zoujingli/typeapp/actions/runs/36235794593)复现了相同故障：20 秒预算在 20.218 秒触发内层超时，退出码 `-1073741510`，标准错误为 `^C`。仅调整等待预算后，同一程序在两个新建空库分别于 22.145 秒和 22.056 秒正常退出，两次均核对 83 个前端文件摘要；数据库采样没有锁等待。数据库实例及目录精确清理通过。修复提交 `4197eeb200251b6dd4f9dae7ac45125239e3038b` 将候选初始化统一到现有应用测试的 30 秒预算，并在成功报告和失败消息中记录内层退出、耗时及超时状态；原 RC3 标签保持不变，测量不能替代完整发布验收。

[修复后的原 ZIP 完整回归](https://github.com/zoujingli/typeapp/actions/runs/36236130040)通过。初始化在 30 秒预算内耗时 21.644 秒，退出码为零且未超时；运行库完整审计、83 个内嵌文件摘要、重复安装及强制更新、上传文件保留、GET/HEAD 缓存语义、平台与客户登录、站点默认值、角色 CRUD、篡改拒绝和正常停止均完成，专用 PostgreSQL 实例清理通过。它仍是 RC3 原件的补验，不改变原发布失败结论。

macOS ARM64 原候选归档 SHA-256 为 `f8c7cecbbff41c718908bf860e36c1e9084da1932eaf1af190c761f7e8fdf24e`，程序 SHA-256 为 `0744b9862cdaa39bd8dc0cfe35549ef61c689acac8dc1047e9b5181f54830e3a`。下载后直接解包，没有重新编译。隔离目录禁止读取主仓业务源码、Composer 与 SDK，并禁止执行 PHP/编译器；安装产生的 83 个文件逐项核对大小和摘要通过。全部文件的 GET 正文摘要、HEAD 空正文及 Content-Length、MIME、ETag 和缓存策略通过，首页 HEAD 和真实 `/public/site` JSON 也通过。

同一程序的浏览器补验通过平台及客户登录/退出、租户工作区、站点默认值、设置保存后刷新回读，以及平台角色新增、修改、查询、真实 API 删除和页面刷新确认。浏览器没有运行时异常，保留 HTTP 结果、操作回执及页面截图。这是本机 macOS 27 的功能补验；完整 dyld 部署审计仍因系统缓存与 CI macOS 15 不同而失败，不以页面成功覆盖该限制。

当前交付为包含原生运行库的目录归档。前端编入主程序，在 `app:install` 或 `web:install` 时安装到应用根下的 `public/`；普通启动不释放资源。完整静态单程序目标仍未完成，外部数据库及其他已配置的业务服务仍需准备。RC 不是稳定版本。

## RC4 原候选补验

不可变标签 `v1.0.0-rc.4` 指向 `fedc731d5a3c288e79c08af8b7f7f88c3408aa87`，由[独立正式运行](https://github.com/zoujingli/typeapp/actions/runs/36236424786)重新生成共享前端与四平台产物，按同一最终归档完成各自验收后才进入分发。旧候选不会被当作 RC4 附件。共享前端为 83 个文件，清单 SHA-256 为 `0d2e93a55f2f73e926c11d9cdf1f50d3a0b15c9baf93f48438f78575a025c61f`。

四个平台的原候选归档已从本轮 Actions 下载，逐文件摘要、ELF/Mach-O/PE 机器类型、配置示例、安装手册与许可材料均核对通过。归档不含外置前端、业务 PHP 源码、Composer 或编译 SDK；83 个内嵌资源条目与共享前端清单完全一致。

| 平台 | 原归档 SHA-256 |
| --- | --- |
| Linux x64 | `f39546b80d35bb683d21304988eae9ef8a41277b7b987e60c082620923f43f59` |
| Linux ARM64 | `dff4e65e87ddb77cf83621f64b96da2dc15df89584c94973e3c427a64096175c` |
| macOS ARM64 | `b263db26abc406237111fcd5f2214f14ba3db84ac3eeb8b3afee88e753342eef` |
| Windows x64 | `51dc0c579c6afdf55c8048ff834c684f92882a6809dc9a03da14a17274a32f08` |

已将四份原构建身份与十二份最终归档部署报告逐项关联：每个平台用自己的同一程序和归档完成 MySQL、PostgreSQL、SQLite 验收，涵盖完整部署审计、全部前端摘要、GET/HEAD 缓存、双端登录、站点默认值、角色 CRUD 和正常停止。Windows PostgreSQL 初始化耗时 21.924 秒，在 30 秒预算内零状态退出，未超时。三种 Unix 平台的九份报告均强制禁止读取源码、SDK 与 Composer，以及执行 PHP/编译器；Windows 的三份报告没有同等强度的访问隔离，不能混称通过最严格隔离。

四平台完整矩阵与 `release-native-complete` 汇总门禁全部通过。随后 `candidate` 创建草稿成功，但即时列表回读未找到新草稿，原执行因此失败，未进入子仓分发和公开 Release。后续用相同查询代码确认草稿存在，源码、原运行及轮次正确，尚无附件；[从原 tag 手动重试](https://github.com/zoujingli/typeapp/actions/runs/36244423079)，按草稿身份复用原运行 `36236424786` 第 1 轮的已验收归档。此重试不重新编译、不移动标签，后续消费及公开仍以实际结果为准。

重试已通过候选恢复：六个附件成功上传，四份归档摘要与原验收字节一致。针对创建成功后列表暂不可见、公开后暂仍显示草稿的情况，`main` 发布工具新增最多五次读取、总计十五秒退避，不重复写入；API 错误直接传播，超预算仍失败。命令边界的旧视图回归在原实现下复现“创建Release后无法回读”，修复后发布契约 39 个测试、48 个断言通过。该工具修正不修改 RC4 标签、程序或本次固定 tag 的工作流。

回读修正提交为 `5955704`，完整单元通过 140 项、3162 个断言，格式、基础和真实 Git 分发检查通过。RC4 重试完成了 15 个组件 tag 分发与汇总，随后停在 Packagist 等待：包详情 JSON 只报告 1/15 个版本，但实际 Composer v2 静态索引已有全部版本。抽查 webhook 的标签创建事件收到 HTTP 202，并返回更新任务编号；不是子仓缺少 tag。

[Packagist 官方 API 文档](https://packagist.org/apidoc#get-package-data)明确说明包详情 API 缓存十二小时，并推荐 Composer v2 静态元数据。相同组件的详情响应缺少 RC4，静态索引与带查询参数的新详情响应均包含 RC4。原 `Packagist::wait()` 对该真实组件在一秒预算内复现超时；提交 `5543b0e` 改用静态索引，并复用 `composer/metadata-minifier` 官方差量展开实现。修正后逐项核对全部 15 个 RC4 组件的版本与拆分 SHA 通过；148 项单元、3170 个断言，以及格式、784 文件基础检查和真实 Git 分发检查通过。新库仅为发布工具的开发依赖，不进入生产 PHP 输入。

为使工具修正与正式 tag 的源码身份一致，停止 RC4 的缓存等待，后续使用新的不可变 RC5 重新编译和验收；RC4 草稿、六个附件和已成功的 15 个子仓 tag 保留，不移动或替换。RC4 的模板分发、公开消费和 17 个 Release 尚未闭合，不能把原生矩阵成功写成完整公开成功。

进一步实测 GitHub 列表响应包含 `Cache-Control: private, max-age=60, s-maxage=60`。提交 `34efa11` 显式请求缓存重新验证，并把写入后回读调整为最多七次、累计六十三秒退避，覆盖该窗口；47 项发布和索引契约、56 个断言及格式检查通过。正常取消等待超过五分钟后，使用该次运行的强制取消入口收尾；最终状态为 `cancelled`，不计为验收成功。

macOS ARM64 原候选归档 SHA-256 为 `b263db26abc406237111fcd5f2214f14ba3db84ac3eeb8b3afee88e753342eef`，程序 SHA-256 为 `3aa4c8b976bdc916edec2e60fd769a64a1c46dc890f27a429ab5984fc4920f4d`。本机直接解包该原件，在含空格的独立运行目录初始化 SQLite 并安装页面；沙箱禁止读取主仓源码、Composer 与 SDK，以及执行 PHP/编译器。83 个文件的落盘摘要、GET 正文、HEAD 长度、MIME、ETag 和缓存策略通过，平台与客户登录退出、租户上下文、默认站点、设置保存回读及角色增改查和 API 删除回读通过，浏览器无运行时异常。四张稳定页面截图经实际检查。

第一次浏览器补验在跨身份域切换时过早填写密码，导航完成后密码被页面清空，未发出客户登录请求。最小复现中未等待的三次有两次失败；等待客户登录 URL 和对应表单就绪后三次均通过，完整补验重跑也通过。只修正临时验收脚本，没有修改或重新构建候选程序。原失败回执保留。macOS 27 的完整 dyld 部署审计仍因系统缓存与 macOS 15 构建基线不同而拒绝，功能补验不扩大平台支持范围。

## 证据保全

发布前四平台的原始日志、运行元数据与观察记录保存于 `.cache/retained-evidence/frontend-release-20260926/pre-release-ci.tar.gz`，SHA-256 为 `56c6024f2e66d86659558be6062979a31396af045c0572144822cf676f37612c`。10 个原始文件已逐项回读；恢复时先核对归档摘要，再在新建任务目录按 `evidence-files.json` 校验所需条目。

`rc.1` 的失败记录另存 `rc1-failure.tar.gz`，SHA-256 为 `c0c87256e42373c763f63406838985e072544883eb07626121e16a2206077f68`，191 个原始文件逐项回读通过。归档包含运行状态、失败日志、三个非 Windows 候选原归档、Windows 原程序及构建身份、修复前后的路径回归和取消回执。Windows 首次失败的候选 ZIP 及内层 SQLite 日志未进入原 Actions 附件，不能事后重建并声称是原件；这也是此次补齐失败证据范围的原因。

`rc.2` 原件保存在同目录的 `rc2-rejected.tar.gz`，SHA-256 为 `29cb0c5d3012d69012d62ab417c12aa8e9c1fa26653feef8cc0fd35b26286258`。109 个文件逐项回读通过，包含三个已完成候选、共享前端、Actions 日志和状态、浏览器补验、真实 `HEAD` 失败响应及未分发回执。验收服务零状态退出，临时解包和运行目录回收，释放 354116272 逻辑字节。

`HEAD` 修复的本机证据保存为 `frontend-head-fix.tar.gz`，SHA-256 为 `53fd582eba50e8dde8a9b48a074f56bc7d22ff95f300776ff4e905c8b7c7bd11`。15 个原件逐项回读，包含源码补丁、构建身份、检查日志及原运行包；回收本轮编译、缓存和搬迁目录，释放 365927006 逻辑字节。

`rc.3` 的本机浏览器补验保存为 `rc3-browser-evidence.tar.gz`，SHA-256 为 `8dc3e7b7d6f096d3edbb416139f25235a90e49d1ac5d75c468e3419b54601af9`，30 个原件逐项回读通过。验收服务零状态退出，监听端口释放，临时解包和运行目录回收，释放 71370714 逻辑字节；原候选归档保留到正式发布核对完成。

`rc.3` 的三平台候选、最终归档三库报告、共享前端、Windows 原始失败附件、日志和取消回执保存为 `rc3-rejected.tar.gz`，SHA-256 为 `041ad42c7a44736f2ae2b93f0496a3b1b8a6fbd967dd9587a70e2ee1450972ec`，173 个原件逐项回读通过。Windows 原附件内已包含其失败 ZIP，无需再保留一份独立副本；后续诊断从原 Actions 附件获取同一字节，不复用其他平台的成功结果作为 Windows 通过证据。

Windows 安装预算的测量保全为 `windows-install-budget-measurement.tar.gz`，SHA-256 为 `052e3eb1e403ad21f31cb130f8b61592ad68a8d4de653d0241b5b601b32f1220`，17 个文件逐项回读；修复后原 ZIP 的完整回归保全为 `windows-install-budget-replay.tar.gz`，SHA-256 为 `cafd67fd7e7e1a6cf44c20d6d93ad386316b0fe85c33ab17ff25a5315b46b152`，16 个文件逐项回读。RC3 本地候选与重复解包文件在核对归档及进程归属后回收，释放 332035311 逻辑字节，SDK 保留供后续验收。

RC4 浏览器补验保存在同目录的 `rc4-browser-evidence.tar.gz`，SHA-256 为 `514d1b0e81676bbcab36d9ecd58d89ec752a1c02726fa84a77b57cbcae16c5a1`，36 个原件逐项回读通过；首次脚本失败另存 `rc4-browser-initial.tar.gz`，SHA-256 为 `116c270ffd2ca3e94c6049b6370f94ebf5b80219af757de0cc15094573520226`，5 个原件回读通过。服务零状态退出、进程及监听端口回收，最终隔离目录清理释放 71427659 逻辑字节。

RC4 四平台原候选、共享前端、原构建与三库验收报告保全为 `rc4-native-candidates.tar.gz`，SHA-256 为 `71b3d7ca1e800957324c5697d0843c3de6fca73e30e868c38ac94935e3a57f79`，234 个原件逐项回读通过。公开下载的后续回读须匹配这些原归档摘要，不用重新构建替代。

RC4 首轮完整工作流日志、全部任务状态、候选失败和重试前草稿身份保全为 `rc4-original-workflow.tar.gz`，SHA-256 为 `26d1f932e65a30fe2b03e87c48a0b36ecd710a249825768ed404ce8877bab34d`，8 个原件逐项回读通过。其中原 GitHub 日志 ZIP 包含 153 个文件，CRC 校验通过；矩阵成功与候选失败分别保留。

回读重试修正保全为 `release-readback-fix.tar.gz`，SHA-256 为 `7770321fac73e3ca4e863f6621a81612fbc61d587c0a7528a334149e27d8bfa5`，7 个原件逐项回读。静态索引修正保全为 `packagist-static-index.tar.gz`，SHA-256 为 `bc50719b58cd864cc30de474c9f070acfc4a38a656df896b556db97edf914884`，13 个原件逐项回读，包含官方文档、真实旧响应、新索引与十五组件来源、修复补丁及检查日志。

缓存窗口修正保全为 `release-cache-window.tar.gz`，SHA-256 为 `89bebb3a13a8d998f4501e7a2bc3b7b4a5166c330d1e1f09402974351f17512a`，3 个原件逐项回读。RC4 的部分分发保全为 `rc4-partial-distribution.tar.gz`，SHA-256 为 `da6dfa74170f82ad11910f0ca3c495df06d763c919909192a96fb1791e2fe71a`，14 个原件逐项回读，包含原批次计划/结果 ZIP、完整重试日志、取消回执和未公开草稿的六个附件身份。

RC5 前置文档保全为 `rc5-pre-tag-docs.tar.gz`，SHA-256 为 `72fd4a5cf832d9909933c5ec6b7c32e2e05204317cc2da5f51adc5fe82118c11`，32 个原件逐项回读。包含导出站点、六页十二个桌面与窄屏视图、源码差异、GitHub 缓存响应头及十七仓无同名 tag/Release 的预检。标题、图片、流程图和页面宽度检查通过，没有部署站点。核对归档及进程占用后回收 RC4 原候选下载副本、共享前端副本和本次站点导出，释放 209456141 逻辑字节；原件仍在保全归档，SDK 保留。

RC4 发布前的文档预览保存在 `rc4-docs-before-public.tar.gz`，SHA-256 为 `2c9ea653606cc3bfe43d82b6f984fad81ce799b49f8804077ab54bfa57413be3`，176 个原件逐项回读通过。覆盖 26 页、52 个桌面与窄屏视图，另含新增真实管理页面截图及安装教程的定向预览；原始 Markdown、未提交差异、截图来源和检查日志均保留。两个临时导出目录回收，释放 9142945 逻辑字节；预览端口均已释放，没有部署站点。

发布期间另核对 Docsify 首页、安装与环境说明、系统架构及全部组件页，共 25 页的桌面与窄屏视图。过宽流程图改为纵向，教程调用时序拆成入口与事务视角；修订后图示可在桌面正文完整展示，窄屏保留图框内滚动。文档检查通过，无浏览器运行时错误，标题保留“物联开源分享”。原始报告、图示截图及对应源码差异保存在 `rc3-docs-components-layout.tar.gz`，SHA-256 为 `7bc03b88ef60658e23da40a4dbcf5329869f6cb8c839460fcd7d5f71f17fd63d`，55 个文件逐项回读；预览服务与导出目录已回收。这些文档改动不改变 RC 原标签的源码身份，也没有部署业务或文档站点。

本机全量编译、内嵌安装、浏览器操作及缓存补验保留在[本机验收记录](frontend-release-20260926.md)，不改写为上述 tag 的产物身份。
