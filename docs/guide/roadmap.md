# 当前能力与实现规划

TypeApp 以 TypePHP 编译生产代码，以 Swoole 承担通信与基础并发，通过 Plugins 组合应用能力。本文将代码中已有的入口与尚未完成的交付要求对应起来；源码发布并不表示全部目标平台、协议义务和部署形态已经验收。

## 当前实现

- 已有 HTTP、TCP、UDP、MQTT、WebSocket 的入口与独立教程。HTTP 和 WebSocket 可以在同一服务、同一端口中分别处理普通请求和协议升级，接入方式见[WebSocket](communications/websocket.md)。
- Plugins 提供运行时、核心服务、三库 ORM、Redis、缓存、校验、日志、队列、调度、构建、测试及 MQTT 能力。MySQL、PostgreSQL、SQLite 按各自真实语义验证。
- 物联中心具有平台账号、客户账号、租户成员与 RBAC；Broker 使用独立管理身份域。组件与物联业务分别维护。
- 构建工具已有生产输入清单、依赖身份、AOT 构建、携带运行库的目录包和归档能力。单程序封装仍待完成。

各平台已通过的组件、HTTP、SQLite 与命令场景统一记录在[平台与验收](platforms.md)。Linux x64 基础命令、ARM64 场景与 Windows 组件结果分别成立，完整应用、全部通信和单程序交付仍按下表推进。

架构整理采用“标准共用、协议独立、显式装配”：继续复用 PSR、Swoole、`ExecutionScope` 和 `ManagedResource`，不新增通用 DI、统一 Transport/Server 管理器或运行时扫描层。优化只在真实边界补接口和统计，HTTP/WebSocket 共用服务、TCP、UDP、MQTT 均按同一生命周期约定验收。

## 待完成工作与验收条件

| 工作 | 当前代码边界 | 完成条件 |
| --- | --- | --- |
| Model 自动连接与主从路由 | 已有受管池和显式读写会话；Model 仍需传 Connection，现有读写会话具有写后粘主行为，应用尚未装配从库 | 实现无参 Model CRUD、默认读从写主、`master()` 主读、事务同连接、事务外不自动粘主及从库故障明确失败；完成上下文与三库 PHP/AOT/独立消费验收，规则见[模型连接与主从路由](../development/model-connections.md) |
| 物理数据库连接复用 | 当前归还关闭 PDO，只复用逻辑槽位 | 在既有池内安全复用干净物理连接，真实验证会话重置、污染隔离、凭据轮换、等待与资源回收，不提前释放仍在使用的连接 |
| 领域 Model 与业务 CRUD | 身份/租户正在建立领域 Model；业务仍有直接表访问，模型边界静态检查尚未落地 | 先完成身份/租户的模型、服务、迁移/夹具、授权主读、HTTP 回归、三库与文档，再覆盖其他领域；限制表查询例外，保持租户与版本语义 |
| Swoole 通信与并发验收 | 代码已统一由 Swoole 承担 Broker、客户端、持久工作、设备授权和 CRL HTTPS；仍需按产物完成协议与平台组合验收 | 验证 TLS、QoS、保活、重连、取消、资源释放、持久确认和 CRL 更新，并记录同一产物证据 |
| 按平台能力选择执行方式 | 入口根据 Swoole 构建能力选择 Process、Thread 或 Coroutine；各角色仍需逐平台验证隔离、容量与停止 | 按官方能力运行对应角色，记录状态隔离、容量、停止和资源回收证据 |
| SDK 与编译线程接入 | 各目标平台需锁定匹配的 PHPX、Swoole、线程/协程开关和生产扩展 | 固定版本、模块摘要和构建开关，完成线程生命周期、协程降级与完整应用 AOT 验收 |
| 单程序加配置交付 | 当前 NativePackage 为目录包，PackageArchive 为归档 | 生成一个程序文件与外置配置，程序自动管理原生库；在干净目标机器验证启动、搬迁、权限、故障和清理 |
| 标准物联项目分发 | 独立应用模板与完整主仓业务生成范围仍需汇合 | 从同一业务、前端与配置源生成，使用真实公开组件安装，完成初始化、升级与重装 |
| 双端管理体验 | 身份/RBAC 基础已存在，部分页面仍使用应用自定义外观封装 | 按锁定 Vben 能力完善导航、登录、表格、表单、主题与个人偏好，并验证权限和浏览器实际行为 |
| MQTT 协议与高可用验收 | 已有协议、互操作、持久恢复与容量测试入口，完整规范义务和故障域验收未闭合 | 对 MQTT 3.1.1/5.0 逐条建立断言与当前产物运行结果，覆盖传输、集群、目标规模与故障恢复 |
| 业务组合测试对齐 | 部分恢复、接入及运维测试仍引用已移除的身份或命令入口 | 将装置统一到当前双端身份与命令，保留持久确认、隔离、恢复和故障断言，再执行完整组合回归 |
| 全平台完整验收 | 已有按平台记录的命令、组件和通信场景；Windows 后续完整应用及三库步骤尚未执行，ARM64 最新平台验收未通过完整应用 | 补齐 SDK 后，Linux x64/ARM64、macOS ARM64、Windows x64 分别用同一产物验证全量生产编译、五种通信、三库语义与无源码部署 |

## 推进顺序

先锁定匹配的 Swoole 模块与编译线程/协程 SDK，完成通信与执行入口验收，并验证完整应用 AOT；随后完成单程序封装和标准项目生成，执行独立消费及目标平台的协议、三库、部署组合验收。UI 与业务完善按现有稳定接口推进，说明只保留当前实现和验收条件。

每项结果以实际命令退出、协议响应、业务状态和资源释放为判断依据。PHP 行为检查、AOT 编译成功、原生行为通过和完整交付分别判定；缺少运行结果的能力明确保留待验收状态。
